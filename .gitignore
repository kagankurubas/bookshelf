# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env

# Symlink farm to .agents/skills/ (real source of truth); duplicate blobs, not real content
.claude/skills/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Installed skills, reproducible from skills-lock.json (kept tracked as provenance);
# security-walls is a project skill with no upstream, so it is tracked
.agents/skills/*
!.agents/skills/security-walls/
